# .env.example  --  Copy to .env and fill in your real keys.
# .env is gitignored and must never be committed.

# Google Gemini API key (required for Route 3 VLM fallback)
# Get yours at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# NOTE: Porcupine has been replaced by openWakeWord (no key required).
# Wake word "hey jarvis" works out of the box after running:
#   python -c "from openwakeword.utils import download_models; download_models()"
